const EventEmitter = {
	events: {},
	on(event, cb) {
		(this.events[event] ??= []).push(cb);
	},
	emit(event, data) {
		(this.events[event] ?? []).forEach(cb => cb(data));
	},
};
EventEmitter.on("data", d => console.log(d));
EventEmitter.emit("data", 42);
